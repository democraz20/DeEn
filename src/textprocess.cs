namespace DeEn {
    //lock code padding count will be at end of file
    class Decryptor{
        public static Tuple<String, String> strip(string input) {
            string lastchar = input.Substring(input.Length-1);
            return Tuple.Create("", "");
        }
    }
    class Encryptor{

    }
}